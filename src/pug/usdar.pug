doctype html
html(lang="es" data-bs-theme="dark")
    head
        //- <meta name="viewport" content="width=device-width, initial-scale=1">
        meta(name="viewport" content="width=device-width, initial-scale=1")
        link(rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css")
        block css
        link(rel="stylesheet" href="css/style_ar.css")
        .logo(style="background-image: url('img/arlogo.png')")
    header.sticky-top
        .nav_container
            nav
                ul
                    li
                        a(href="#") Soporte
    body
        .mt-5
            h2.text-center Pasarela de pago 
                strong.text-primary USDAR

            .container.mt-5
                .row
                    .col-md-12
                        h2.text-center Valor a pagar
                        input#amount.form-control( placeholder="Monto a pagar" value="" style="font-size: 2rem; text-align: center;")
                        hr
                    .col-md-12.d-flex.justify-content-center.align-items-center
                        button.btn.btn-primary.btn-lg.btn-block.me-5 Aprobar pago
                        //- subrayado
                        button.btn.btn-lg.btn-block.btn-link Cancelar

        .login.mt-5.d-none
            .container
                .row
                    .col-md-12(style="max-width: 350px; margin: 0 auto;")
                        h2.text-center Iniciar sesión
                        form
                            .mb-3
                                label(for="email") Correo electrónico
                                input#email.form-control(type="email", placeholder="Correo electrónico")
                            .mb-3
                                label(for="password") Contraseña
                                input#password.form-control(type="password", placeholder="Contraseña")
                            button.btn.btn-primary.btn-lg.btn-block Iniciar sesión

    footer#footer.mt-5
        .container
            .row
                .col-md-12
                    .footer-logo(style="background-image: url('img/arlogo.png')")
                    p © 2024 ArWorld. Todos los derechos reservados.
                    hr
                .col-md-4
                    h5 Contacto
                    ul
                        li
                            a(href="tel:+1234567890") +1-234-567-890
                        li
                            a(href="mailto:info@ArWorld.com") info@ArWorld.com

                .col-md-4
                    h5 Empresa
                    ul
                        li
                            a(href="/about") Sobre nosotros
                        li
                            a(href="/privacy") Política de privacidad
                        li
                            a(href="/terms") Términos de servicio

                .col-md-4
                    h5 Síguenos
                    ul.social-links
                        li
                            a(href="https://facebook.com/ArWorld") 
                                img(src="images/facebook.svg" alt="Facebook")
                        li
                            a(href="https://twitter.com/ArWorld") 
                                img(src="images/twitter.svg" alt="Twitter")
                        li
                            a(href="https://instagram.com/ArWorld") 
                                img(src="images/instagram.svg" alt="Instagram")


    script(src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js")

    script.
        document.getElementById('amount').addEventListener('input', function() {
            //- si coloca punto lo deja colocar 2 decimales
                         
            // Obtener el valor actual del input
            let inputValue = this.value;

            // Limpiar el input dejando solo números y un punto decimal (eliminar puntos adicionales y comas)
            inputValue = inputValue.replace(/[^0-9.]+/g, '').replace(/(\..*)\./g, '$1');

            // Verificar si hay una parte decimal
            let parts = inputValue.split('.');
            let integerPart = parts[0];
            let decimalPart = parts[1];

            // Revisar si la parte entera necesita formateo de miles
            integerPart = integerPart.replace(/\B(?=(\d{3})+(?!\d))/g, ',');

            // Combinar la parte entera y la parte decimal (si existe) con formato correcto
            this.value = decimalPart ? `${integerPart}.${decimalPart.substring(0, 2)}` : integerPart;
        });